- name : testing variable precedence
  hosts : mongodb
  vars :
    NAME: harish-play
  vars_fies:
  - variable.yaml
  vars_prompt:
  - name: NAME
    prompt: please enter trainer name
    private: false
  tasks :
  - name: print at task level
    vars: 
     NAME: harish-task
    ansible.builtin.debug:
      msg : "Trainer name is {{NAME}}"